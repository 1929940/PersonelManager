<Page x:Class="Desktop.UI.Business.Login.ChangePasswordPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Desktop.UI.Business.Login"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Nowe hasło:"/>
        <PasswordBox x:Name="PasswordBox"
            Grid.Row="0" Grid.Column="1" Margin="5" VerticalContentAlignment="Center"/>

        <Label Grid.Row="1" Grid.Column="0" Content="Powtórz hasło:"/>
        <PasswordBox x:Name="PasswordBox2" 
            Grid.Row="1" Grid.Column="1" Margin="5" VerticalContentAlignment="Center"/>

        <Button Name="ChangeButton"
            Grid.Row="3" Grid.Column="1" Content="Zmień" 
                Margin="5" Width="100" HorizontalAlignment="Right"
                Click="ChangeButton_Click"/>

    </Grid>
</Page>
